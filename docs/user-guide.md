# User Guide

1. Start backend service.
2. Visit `/docs` to explore endpoints.
3. Request `/api/v1/market/overview` for snapshot.
4. Connect to `/ws/stocks/AAPL` for live updates.
5. Inspect indicator fields for anomaly flags.
